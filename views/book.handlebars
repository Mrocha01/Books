<div class="book-page">
   <div class="container">
        <h1>{{book.title}}</h1>
        <p>Este livro tem {{book.pageqty}} páginas.</p>
   <a href="/books" class="btn-add-book">Voltar</a>
   <a href="/books/edit/{{book.id}}" class="btn-add-book" id="btn-books">Editar</a>
   <form action="/books/remove/{{book.id}}" method="post">
      <input type="submit" value="Remover" class="btn-remove-book">
   </form>
   </div>
</div>